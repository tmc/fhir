version: v1
managed:
   enabled: true
   go_package_prefix:
     default: github.com/tmc/fhir/gen
     except:
      - buf.build/googleapis/googleapis
plugins:
  - name: d2
    out: .
    opt: paths=source_relative
